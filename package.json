{
  "name": "kiram-com",
  "private": true,
  "description": "Trion Pay / Kiram.com — Mobil + Web fintech platformu. Monorepo.",
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web"
  },
  "workspaces": [
    "apps/web",
    "apps/admin",
    "packages/*"
  ],
  "overrides": {
    "expo": "~54.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
